# Demo mode - no external services needed
# All data is served from in-memory mock data
# JWT_SECRET, database, B2, and email configuration are not used

# DEMO_ROLE selects which user is auto-logged in:
#   unset or "admin"  -> Coach Marcus Rivera (instructor dashboard, full admin)
#   "student"         -> Jake Thompson (student dashboard, his 6 sessions)
# DEMO_ROLE=student
